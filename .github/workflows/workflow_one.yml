 name: workflow_one

on:
  push:

jobs:
  job1:
    name: Job1
    runs-on: ubuntu-latest

    steps:
      - name: Step-1
        run: |
          pwd
          ls -ltr
          hostname

      - name: Step-2 - Checkout
        uses: actions/checkout@v7

      - name: Step-3
        run: |
          pwd
          ls -ltr
          hostname
